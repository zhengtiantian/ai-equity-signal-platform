MYSQL_ROOT_PASSWORD=change-me
MONGO_ROOT_PASSWORD=change-me
KEYCLOAK_DB_PASSWORD=change-me
KEYCLOAK_ADMIN_PASSWORD=change-me
AIRFLOW_POSTGRES_PASSWORD=change-me
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=change-me
AIRFLOW_WEBSERVER_SECRET_KEY=change-me
AIRFLOW_ADMIN_PASSWORD=change-me

# Finnhub API key — live quotes on the holdings page. Optional:
# without it the page falls back to the last daily close.
FINNHUB_API_KEY=
